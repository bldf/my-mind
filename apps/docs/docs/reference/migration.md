# Migration and Breaking Changes

Before `1.0`, breaking changes are allowed only when they improve the stable API
surface or remove unsafe behavior. Every breaking change must include:

- A changeset with the affected packages.
- A migration section in release notes.
- Updated API reference stability labels.
- Example updates for Vite and Next.js where relevant.
