# Public Beta Report

## Release Scope

The beta includes core schema validation, commands, history, React editor/viewer,
outline editing, search, inspector, optional import/export packages, docs, and
static examples.

## Known Limits

- PNG export requires a browser Canvas implementation.
- Complex mobile drag and provider/plugin APIs remain experimental.
- Package size budgets require CI verification after dependencies are installed.
